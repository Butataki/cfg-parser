from setuptools import setup

setup(
    name="CfgParser",
    version="1.0.0",
    packages=['cfg_parser'],
    # metadata
    author="Roman Shuvalov",
    author_email="orangato@yandex.ru",
    description="Parsing config files with predetermined structure and generating default one if no config file found.",
    keywords="configuration cfg config",
)